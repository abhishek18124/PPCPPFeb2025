#include<iostream>
#include<climits>

using namespace std;

int main() {

	// cout << INT_MIN << endl; // -inf
	// cout << INT_MAX << endl; // inf

	int x;
	cout << x << endl; // don't do this as x contains garbage value

	// before using a variable make sure you either read a value into it
	// or assign a value to it

	return 0;
}